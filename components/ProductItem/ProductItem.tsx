interface IProduct {
  id: string;
  photo: string;
  type: string;
  brand: string;
  name: string;
  sale_tag?: string;
  saled_price?: number;
  price?: number;
}

const ProductItem = (props: IProduct) => {
  return (
    // <div className={styles.wrapper}>
    //   <div className={styes.img}>

    //   </div>
    // </div>
  )
}